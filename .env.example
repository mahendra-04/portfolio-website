# Optional Web3Forms key used by the contact form in src/App.jsx.
# Leave this as a placeholder in the example file. Real keys should live in a
# local .env file that is not committed. When the key is absent, the portfolio
# safely falls back to opening a prefilled email draft through mailto.
VITE_WEB3FORMS_ACCESS_KEY=your_web3forms_access_key_here
